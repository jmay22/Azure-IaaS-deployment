# Azure-IaaS-deployment
build a classic "IaaS" architecture.Made a Virtual Network with two subnets: a "Public" subnet for a Web Server and a "Private" subnet for a Database Server. I configured Network Security Groups (NSGs) to ensure the Database Server is protected from the internet and can only be accessed by the Web Server.
